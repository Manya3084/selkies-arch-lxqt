#!/bin/bash
export XDG_RUNTIME_DIR="${XDG_RUNTIME_DIR:-/tmp/runtime-ubuntu}"
until [ -f "${XDG_RUNTIME_DIR}/container-env" ]; do sleep 0.5; done
. "${XDG_RUNTIME_DIR}/container-env"
exec /etc/selkies-entrypoint.sh
