diff --git a/pixelflux/src/encoders/vaapi.rs b/pixelflux/src/encoders/vaapi.rs
index 2600e53..7c81b83 100644
--- a/pixelflux/src/encoders/vaapi.rs
+++ b/pixelflux/src/encoders/vaapi.rs
@@ -4,8 +4,8 @@
  * file, You can obtain one at https://mozilla.org/MPL/2.0/.
  */
 
-//! Hardware-accelerated H.264 encoding on VA-API through FFmpeg's `h264_vaapi`
-//! encoder. Frames reach the GPU by one of three entry points — a Wayland
+//! Hardware-accelerated H.264 / AV1 encoding on VA-API through FFmpeg's
+//! `h264_vaapi` / `av1_vaapi` encoders. Frames reach the GPU by one of three entry points — a Wayland
 //! DRM-PRIME dmabuf (`encode_dmabuf`), a host BGRA frame from the X11 capture
 //! path (`encode_host_argb`), or already-planar NV12 pixels (`encode_raw`) — and
 //! an FFmpeg filter graph runs VA-VPP (`scale_vaapi`) to land the pixels as NV12
@@ -177,6 +177,7 @@ pub struct VaapiEncoder {
     current_kf_s: f64,
 
     omit_stripe_headers: bool,
+    output_mode: i32,
 }
 
 /// Assert `VaapiEncoder` is `Send`: its raw FFmpeg pointers are owned exclusively and the
@@ -227,6 +228,18 @@ impl Drop for VaapiEncoder {
     }
 }
 
+fn vaapi_codec_name(output_mode: i32) -> &'static str {
+    if output_mode == 2 { "av1_vaapi" } else { "h264_vaapi" }
+}
+
+fn vaapi_profile(output_mode: i32) -> &'static str {
+    if output_mode == 2 { "main" } else { "high" }
+}
+
+fn vaapi_level(output_mode: i32) -> &'static str {
+    if output_mode == 2 { "4.0" } else { "4.1" }
+}
+
 impl VaapiEncoder {
     /// Build a VA-API encoder for the Wayland **dmabuf** path — the source is a DRM-PRIME
     /// dmabuf that the filter graph `hwmap`s onto a VA surface. Thin wrapper over `new_impl` with
@@ -353,16 +366,21 @@ impl VaapiEncoder {
                 return Err("Failed to init DRM frames ctx".into());
             }
 
-            let codec_name = CString::new("h264_vaapi").unwrap();
+            let codec_name_str = vaapi_codec_name(settings.output_mode);
+            let codec_name = CString::new(codec_name_str).unwrap();
             let codec = ff::avcodec_find_encoder_by_name(codec_name.as_ptr());
             if codec.is_null() {
                 ff::av_buffer_unref(&mut drm_frames_ref);
                 ff::av_buffer_unref(&mut hw_device_ctx);
                 ff::av_buffer_unref(&mut drm_device_ctx);
-                return Err("h264_vaapi encoder not found".into());
+                return Err(format!("{codec_name_str} encoder not found"));
             }
 
-            let aligned_width = (width + 15) & !15;
+            let aligned_width = if settings.output_mode == 2 {
+                (width + 31) & !31
+            } else {
+                (width + 15) & !15
+            };
             let aligned_height = (height + 31) & !31;
 
             let mut enc_frames_ref = ff::av_hwframe_ctx_alloc(hw_device_ctx);
@@ -436,8 +454,8 @@ impl VaapiEncoder {
                 set_opt(&mut opts, "qp", &settings.video_crf.to_string());
             }
             set_opt(&mut opts, "async_depth", "1");
-            set_opt(&mut opts, "profile", "high");
-            set_opt(&mut opts, "level", "4.1");
+            set_opt(&mut opts, "profile", vaapi_profile(settings.output_mode));
+            set_opt(&mut opts, "level", vaapi_level(settings.output_mode));
 
             let ret = ff::avcodec_open2(encoder_ctx, codec, &mut opts);
             ff::av_dict_free(&mut opts);
@@ -636,6 +654,7 @@ impl VaapiEncoder {
                 current_vbv_mult: settings.video_vbv_multiplier,
                 current_kf_s: settings.keyframe_interval_s,
                 omit_stripe_headers: settings.omit_stripe_headers,
+                output_mode: settings.output_mode,
             })
         }
     }
@@ -699,8 +718,8 @@ impl VaapiEncoder {
             set_opt(&mut opts, "qp", &qp.to_string());
         }
         set_opt(&mut opts, "async_depth", "1");
-        set_opt(&mut opts, "profile", "high");
-        set_opt(&mut opts, "level", "4.1");
+        set_opt(&mut opts, "profile", vaapi_profile(self.output_mode));
+        set_opt(&mut opts, "level", vaapi_level(self.output_mode));
 
         let ret = ff::avcodec_open2(self.encoder_ctx, self.codec, &mut opts);
         ff::av_dict_free(&mut opts);
diff --git a/pixelflux/src/lib.rs b/pixelflux/src/lib.rs
index 7ca8e2b..c0e51ce 100644
--- a/pixelflux/src/lib.rs
+++ b/pixelflux/src/lib.rs
@@ -1068,7 +1068,11 @@ fn wayland_encode_loop(pool: &WlFramePool, cfg: WlEncodeConfig) -> Option<GpuEnc
     let (mut video_encoder, mut openh264_encoder) =
         build_readback_encoders(&settings, cfg.try_gpu, cfg.prior);
     if cfg.try_gpu && video_encoder.is_none() && openh264_encoder.is_none() {
-        println!("[Wayland] Decision: No GPU Encoder available -> Using CPU Software Encoding.");
+        if settings.output_mode == 2 {
+            println!("[Wayland] Decision: AV1 requires VAAPI (av1_vaapi); no CPU fallback.");
+        } else {
+            println!("[Wayland] Decision: No GPU Encoder available -> Using CPU Software Encoding.");
+        }
     }
     let n_stripes = wayland_stripe_count(
         &settings,
@@ -1222,6 +1226,7 @@ fn wayland_encode_loop(pool: &WlFramePool, cfg: WlEncodeConfig) -> Option<GpuEnc
             if f.is_animated {
                 damage.push(Rectangle::new((0, 0).into(), (width, height).into()));
             }
+            if settings.output_mode != 2 {
             let force_idr_all = requested_idr
                 || (settings.output_mode == 1
                     && crate::pipeline::periodic_idr_due(&settings, f.frame_id));
@@ -1237,6 +1242,7 @@ fn wayland_encode_loop(pool: &WlFramePool, cfg: WlEncodeConfig) -> Option<GpuEnc
                 false,
                 force_idr_all,
             );
+            }
         }
 
         let WlFrame { id, buf, .. } = f;
@@ -1266,6 +1272,12 @@ fn wayland_encode_loop(pool: &WlFramePool, cfg: WlEncodeConfig) -> Option<GpuEnc
     video_encoder
 }
 
+/// Video (not JPEG). Mode 1 is H.264; mode 2 is AV1 (VA-API `av1_vaapi`).
+#[inline]
+fn is_video_mode(output_mode: i32) -> bool {
+    output_mode == 1 || output_mode == 2
+}
+
 /// Compose the encoder half of the 1 s debug log line (backend, colorspace, frame mode) for
 /// whichever thread owns the encoders.
 fn encoder_desc(
@@ -1300,7 +1312,8 @@ fn encoder_desc(
     } else {
         "Striped"
     };
-    format!("H264 ({}) {} {} {} CRF:{}", backend, cs_str, range_str, frame_str, settings.video_crf)
+    let codec_str = if settings.output_mode == 2 { "AV1" } else { "H264" };
+    format!("{} ({}) {} {} {} CRF:{}", codec_str, backend, cs_str, range_str, frame_str, settings.video_crf)
 }
 
 /// Decide how many horizontal stripes a frame is split into, which is really the choice of how
@@ -1313,8 +1326,8 @@ fn wayland_stripe_count(settings: &RustCaptureSettings, fullframe_encoder: bool)
     let num_cores = std::thread::available_parallelism()
         .map(|n| n.get())
         .unwrap_or(1);
-    if settings.output_mode == 1 {
-        if fullframe_encoder || settings.video_fullframe {
+    if is_video_mode(settings.output_mode) {
+        if settings.output_mode == 2 || fullframe_encoder || settings.video_fullframe {
             1
         } else {
             let min_h = 64;
@@ -1360,7 +1373,8 @@ fn log_stream_settings(
                 None => "CPU",
             }
         };
-        log_msg.push_str(&format!(" | Mode: H264 ({})", encoder_type));
+        let codec_str = if settings.output_mode == 2 { "AV1" } else { "H264" };
+        log_msg.push_str(&format!(" | Mode: {} ({})", codec_str, encoder_type));
 
         if video_encoder.is_some() || openh264 || settings.video_fullframe {
             log_msg.push_str(" FullFrame");
@@ -1699,7 +1713,7 @@ fn run_wayland_thread(
                         }
                     }
 
-                    if settings.output_mode == 1 {
+                    if is_video_mode(settings.output_mode) {
                         settings.width &= !1;
                         settings.height &= !1;
                     }
@@ -1817,7 +1831,7 @@ fn run_wayland_thread(
 
                     let use_cpu_explicit = settings.use_cpu || settings.encode_node_index == -1;
                     let gpu_intent =
-                        settings.output_mode == 1 && !settings.use_openh264 && !use_cpu_explicit;
+                        is_video_mode(settings.output_mode) && !settings.use_openh264 && !use_cpu_explicit;
                     if use_cpu_explicit && !(settings.output_mode == 1 && settings.use_openh264) {
                         println!("[Wayland] CPU encoding selected (use_cpu=true or encode_node_index=-1).");
                     }
@@ -1839,7 +1853,7 @@ fn run_wayland_thread(
                             settings.encode_node_index.max(0), encode_driver
                         );
 
-                        if encode_driver.contains("nvidia") {
+                        if settings.output_mode != 2 && encode_driver.contains("nvidia") {
                             let reused = match state.video_encoder.as_mut() {
                                 Some(GpuEncoder::Nvenc(enc)) => {
                                     match enc.reconfigure_resolution(&settings) {
diff --git a/pixelflux/src/pipeline.rs b/pixelflux/src/pipeline.rs
index 6c504c5..0ba3761 100644
--- a/pixelflux/src/pipeline.rs
+++ b/pixelflux/src/pipeline.rs
@@ -201,11 +201,11 @@ impl X11Pipeline {
                     X11Encoder::None
                 }
             }
-        } else if settings.output_mode == 1 && !settings.use_cpu && settings.encode_node_index != -1 {
+        } else if (settings.output_mode == 1 || settings.output_mode == 2) && !settings.use_cpu && settings.encode_node_index != -1 {
             let encode_driver =
                 crate::get_gpu_driver(settings.encode_node_index.max(0));
             println!("[x11] Encode Node Index: {} | Driver: {}", settings.encode_node_index.max(0), encode_driver);
-            if !encode_driver.is_empty() && !encode_driver.contains("nvidia") {
+            if settings.output_mode == 2 || (!encode_driver.is_empty() && !encode_driver.contains("nvidia")) {
                 if settings.video_fullcolor {
                     println!("[x11] 4:4:4 full-color requested. VAAPI does not support this profile reliably. Falling back to CPU.");
                     X11Encoder::None
@@ -236,7 +236,9 @@ impl X11Pipeline {
                 }
             }
         } else {
-            if settings.output_mode == 1 {
+            if settings.output_mode == 2 {
+                println!("[x11] AV1 requires VAAPI (av1_vaapi); no CPU fallback.");
+            } else if settings.output_mode == 1 {
                 println!("[x11] No GPU Encoder available -> Using CPU Software Encoding.");
             }
             X11Encoder::None
@@ -399,6 +401,9 @@ impl X11Pipeline {
                 width as usize * 4,
                 "software encode path assumes tightly-packed rows (stride == width*4)"
             );
+            if self.settings.output_mode == 2 {
+                Vec::new()
+            } else {
             let force_idr_all = requested
                 || (self.settings.output_mode == 1
                     && periodic_idr_due(&self.settings, self.frame_counter));
@@ -414,6 +419,7 @@ impl X11Pipeline {
                 true,
                 force_idr_all,
             )
+            }
         };
 
         self.pending_force_idr = false;
diff --git a/pixelflux/src/x11/mod.rs b/pixelflux/src/x11/mod.rs
index a664bbe..3483c8f 100644
--- a/pixelflux/src/x11/mod.rs
+++ b/pixelflux/src/x11/mod.rs
@@ -222,7 +222,7 @@ fn resolve_dims(root_w: u16, root_h: u16, s: &RustCaptureSettings) -> (u16, u16)
     };
     w = w.clamp(2, u16::MAX as i32);
     h = h.clamp(2, u16::MAX as i32);
-    if s.output_mode == 1 {
+    if s.output_mode == 1 || s.output_mode == 2 {
         w &= !1;
         h &= !1;
     }
